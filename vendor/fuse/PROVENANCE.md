# Vendored: Fuse.js 7.5.0

- File: `fuse.min.mjs` (ESM build) fetched 2026-07-16 from
  `https://cdn.jsdelivr.net/npm/fuse.js@7.5.0/dist/fuse.min.mjs` (26,095 bytes)
- License: Apache-2.0 — full text in `LICENSE` (extracted from the repo clone
  Max downloaded via codebase2txt on 2026-07-16)
- Loaded lazily via dynamic `import()` on first search open; the site's search
  falls back to a built-in substring scorer if this file is missing.
- Update procedure: bump the version in this file + refetch + PR (per
  Cursor_Superprompt_Handover.txt task protocol).
