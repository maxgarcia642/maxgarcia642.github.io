# Superprompt Changelog

Running log of changes — the admin panel auto-appends here on every action.
Paste relevant entries into Cursor when formalizing changes through PRs.

- **2026-07-16** — Full rebuild (v2 "Shapeshifter"): 10-theme token architecture (`themes.css` + `data-theme` + localStorage + inline FOUC guard); Programming section shrunk to mini-terminals; Investment Vehicle Valuations replaced by **The Financial Liberty Project** (`finance.json` data layer, 6 live works with downloadable deliverables in `files/`, 4 coming-soon, dated July-2026 market pulse with honesty flags, live CoinGecko crypto refresh); Pixel Art Studio folded into a **Games & Projects Arcade** with click-to-activate facades; Connect rebuilt as a native link tree (Linktree blocks framing); resume + NWTI reports served through a lazy page-by-page PDF.js viewer (SRI-pinned CDN library); admin.html rewritten on the GitHub Contents API (in-memory fine-grained PAT, rename = delete+create, throttled < 80 writes/min, auto-changelog); meta-CSP + SRI added; **`adminPassword` bcrypt hash removed from content.json** (it was public — client-side password gates are theater); `project-loader.js`, `server.js`, `replit.md`, `package.json` retired (fully static now — no backend).

- **2026-07-16 (post-audit)** — Added `Grok_Superprompt_RepoScout.txt`: repo-scouting brief for Grok (license/weight/static-compat constraints, 9 scouting categories incl. GitHub Actions "Pages maximizers", evaluation rubric, already-vetted exclusion list, handoff-to-Cursor protocol). Requested by Max after delivery review — was not in the original Coding_Prompt_Chat.txt brief (verified: only the Cursor superprompt was requested there); logged here per the no-silent-changes rule.

- **2026-07-16 (v3 expansion — PR #32)** — Major feature round, all by Max's direction. **Themes 10 → 25**: added Vaporwave, Sakura, Midnight OLED, Coffee House, Amber Terminal, Paper Sketch, Synthwave Sunset, Deep Ocean, Desert Adobe, Matcha Garden, Bauhaus, Holographic, Royal Velvet, Arctic Frost, Blueprint (token blocks + registry). **Universal hover/touch FX**: every card/chip/link lifts with an accent-tinted `color-mix` shadow bloom, `:active` covers touch, reduced-motion opts out. **Music**: `track1.mp3` now ships — AIR "Ce matin-là" per Max's explicit call (⚑ licensing flag recorded in assets/music/README.md — commercial recording, DMCA risk accepted knowingly); loops from first interaction, ♫ mutes (remembered), 🎧 plays a visitor's own local file (object URL, never uploaded; CSP media-src + blob:). **Market Pulse 6 → 20 chips**: 6 live crypto (CoinGecko), 6 live FX (Frankfurter/ECB, new CSP origin), commodities verified Jul 16 2026 via Barchart EOD settlements + Finit board (gold $3,992.1 Aug, silver $56.187 Sep, WTI $78.28 Sep, natgas $2.858, copper $6.357, SRW wheat 674'6) — sources + dates in chip contexts; added a converter (currencies/crypto/commodity legs route through USD, refills after live refresh). **Protected doc reader**: DOCX via vendored mammoth 1.11.0 (BSD-2-Clause) + XLSX via vendored SheetJS CE 0.20.3 (Apache-2.0), both LICENSE+PROVENANCE'd and lazy-injected; ALL download links removed from finance works — view-only modal, selection/copy/context-menu blocked; honesty note in SECURITY.md (deterrence, not DRM — raw URLs remain fetchable on a public static host). **Programming terminals** now fetch same-origin (works with the repo private; raw.githubusercontent doesn't) — no credentials in any served file. **NWTI card**: share/direct links removed → one OneDrive redirect (per Max: redirect-only until the compressed PDF lands). **Arcade**: facades show big emoji logos (🎵🌦️⌨️🧩), ℹ️ flaw-hints removed from public UI (moved to README maintainer notes); new cards with doc attachments — Flock Finance, Biotank, DiamondDrive, Winnow (essay + PM plan). **Posts**: renamed "Articles & Posts"; added NWA Meat & Plant (paper + field guide, on-site reader), YouTube channel (redirect, coming soon), Jazz-Fusion/Prog band (coming soon), LinkedIn referral card, and 7 Clawmaxxing coming-soon cards (hub, robot dog, CybersecurityClaw, smart glasses, RetroClawing, hologram, drone — 50+-page papers in generation). **Connect rebuilt**: Linktree hero (Linktree blocks iframes — verified stance retained), branches = LinkedIn/GitHub/YouTube/Handshake (generic URL — Max to supply profile link)/Contact (email+phone from the public resume), redundant project links removed, highlights leaf-chips. **Removed**: `files/` download affordances, embedNote fields' public rendering, old six-link tree. **Pages outage root-caused**: repo went private on the free plan → GitHub Pages disabled → entire site (not just admin) 404'd; repo must stay public, upgrade to Pro, or migrate to Cloudflare Pages. Expand-file bug could not be reproduced in the current build (resume Expand renders 3/3 pages locally) — it was the Pages outage window.

- **2026-07-16 (PR #31 review round)** — Addressed Codacy/Sourcery/CodeRabbit/Qodo/CodeAnt findings on the v2 PR. Security & correctness: CSP `script-src` drops `'unsafe-inline'` for a sha256 hash pinning exactly the FOUC guard (verified in-browser: saved theme still applies pre-paint); admin file list renders paths via `textContent` (stored-XSS fix) and URL-encodes list paths; admin changelog append now runs its whole read-modify-write inside the serialized queue with one 409 retry (stale-SHA race fix); editor SHA invalidates when `editPath` changes; search results built with DOM nodes instead of innerHTML; crypto chip refresh accepts price `0` (`Number.isFinite`); search index reads the real arcade schema (`kind`/`description`) and rebuilds live if the dialog is open; FX resize listener removed on theme-off (leak fix); Math.random → crypto-backed `rand()` for bubble positions (decorative; swapped to satisfy the weak-RNG gate); Ctrl/Cmd/Alt combos no longer hijacked by studio hotkeys. Tokens: `--focus-ring-color` + `--card-border-color` added to all 10 themes so box-shadow/border-bottom consumers get valid values (fixes missing header separators and focus glows). CSS modernized via stylelint (rgb slash notation, `overflow-wrap: anywhere`, `aurora-drift` kebab keyframe); `.stylelintrc.json` documents intentional exceptions (compact single-line declarations, `-webkit-backdrop-filter` needed for Safari); `.codacy.yml` excludes `vendor/`, `files/`, `assets/` from analysis. Pulse: fetch gets a 20s abort timeout, change detection now compares exact numeric price (persisted as `raw`), workflow gains a concurrency group + rebase-before-push. Pixel theme exempts PDF canvases from `image-rendering: pixelated`. Repo tree-link now actually points at the GitHub repo. Declined (with reasons in the PR): sandboxing arcade iframes (breaks itch/game embeds needing scripts+fullscreen+gamepad; frame-src CSP is the control) and full keyboard-operable pixel grid (tracked as a follow-up, not a review-blocker).

- **2026-07-16 (v2 landed in repo)** — PR `v2-shapeshifter` applies the full v2 tree to `maxgarcia642/maxgarcia642.github.io`. Removed from the repo (retired v1 stack, logged here per the no-silent-removal rule): `server.js`, `replit.md`, `package.json`, `package-lock.json`, `project-loader.js`, `SETUP.md`, `.replit`, and the entire `uploads/` folder — the resume there (`uploads/Maximiliano Garcia - Resume.pdf`) is superseded by `files/Maximiliano_Garcia_Resume.pdf`. Kept: v1 `LICENSE` (MIT). Known gaps shipped as-is by design: `files/NWTI_Works.pdf` not yet uploaded (OneDrive fallback active) and `assets/music/track1.mp3` absent pending a licensed track.

- **2026-07-16 (Grok round 1 integrated)** — Audited all 18 repos Max downloaded from the scout run (file-level license verification, not README claims): **adopted** Fuse.js 7.5.0 → vendored at `vendor/fuse/` (Apache-2.0 LICENSE + PROVENANCE.md) powering new site-wide search (🔎 nav button, `/` hotkey, lazy import, substring fallback, deep-scroll via `data-search-id`); **idea-mined** mimopixel → native Pixel Studio v2.1 (PICO-8/Game Boy/Mono palettes, 16/24/32 grids via `--px-cols`, mirror mode + M key, size-aware PNG export) — its README claims MIT but the repo ships NO LICENSE file, so zero code was copied; **rejected with reasons** logged in the Cursor handover ledger, incl. akaiHuang/pixel-art-studio (unlicensed; Grok listed it as "MIT (assumed)") and four GPL/AGPL leaks Grok failed to filter. Added daily market-pulse GitHub Action (`market-pulse.yml` + `scripts/update-pulse.mjs`, GITHUB_TOKEN only, live-verified: BTC chip $63K→$64K, asOf → 2026-07-17); pulse commits are changelog-exempt. Docs updated (README, SECURITY.md, handover ledger, scout addendum). Deduped: removed stale `Grok_Superprompt_Repo_Scout.txt` draft from the interrupted session — `Grok_Superprompt_RepoScout.txt` is canonical.
